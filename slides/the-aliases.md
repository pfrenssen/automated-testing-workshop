## The aliases

```
/**
 * Returns the field alias mapping for news articles.
 *
 * @return array
 */
protected static function newsFieldAliases() {
  return [
    'title' => 'title',
    'body' => 'body',
    'image' => 'field_news_image',
  ];
}
```

